version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/source/conf.py
  builder: html

conda:
    environment: docs/environment.yml

python:
  install:
    - method: pip
      path: .
